# =============================================================================
# PROJECT SETTINGS — change these first
# =============================================================================
COMPOSE_PROFILES=ssl
COMPOSE_PROJECT_NAME=app-skeleton
WEB_PORT=8080
DB_PORT_HOST=3307
REDIS_PORT_HOST=6380
UI_PORT=5173

# Optional canonical URL for docs/scripts
APP_URL=http://localhost:${WEB_PORT}

DOMAIN=app-skeleton.test
ACME_EMAIL=ops@example.com

# =============================================================================
# STANDARD (usually leave as-is)
# =============================================================================
# add project defaults here e.g. infra overrides
